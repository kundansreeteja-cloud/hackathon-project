# Port number the Express server listens on (defaults to 5000 if not specified)
PORT=5000

# Allowed CORS origin(s) - set to your frontend URL in production (e.g. https://your-app.vercel.app)
# Or leave as * for development
CORS_ORIGIN=*
